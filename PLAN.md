## 問題点
- OPENAIからの出力をうまく扱えていない（対応済み：正規化＋RAW出力はデバッグ時のみ）
- 音声特徴量で色を判別して出してるけど色がうまくでない？（対応済み：score_avg を優先）
- 例外処理などをきちんといれてfastAPIが落ちないようにする（対応済み：主要APIで例外処理追加）
- 音声ファイルをメモリ上で扱い続けるのは少し問題なきがする（対応済み：ストリーミング書き込み＋サイズ上限）
- 話者分離（対応済み：/diarize_chunk と /ws_diarize を追加）
- 比較システムが終わっていない

開発上の注意点
- 一つ解決するたびにコミットを細かく切る
- README.md, PLAN.mdをこまめに更新する
- コメントを書く
